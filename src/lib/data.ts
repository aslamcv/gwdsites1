import type { Customer, Invoice, GroundWaterService, Employee } from './types';

export const customers: Customer[] = [];

export const services: GroundWaterService[] = [];

export const invoices: Invoice[] = [];

export const employees: Employee[] = [];
